name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  workflow_dispatch: 

jobs:
  train-model:
    runs-on: ubuntu-latest
    
    # Menambahkan DAGSHUB_USER_TOKEN secara eksplisit agar dideteksi oleh library
    env:
      DAGSHUB_TOKEN: ${{ secrets.DAGSHUB_TOKEN }}
      DAGSHUB_USER_TOKEN: ${{ secrets.DAGSHUB_TOKEN }}
      MLFLOW_TRACKING_USERNAME: ${{ secrets.DAGSHUB_TOKEN }}
      MLFLOW_TRACKING_PASSWORD: ${{ secrets.DAGSHUB_TOKEN }}
      
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install pandas scikit-learn mlflow dagshub matplotlib seaborn

      - name: Run training and tuning scripts
        run: |
          echo "Menjalankan Modelling Utama..."
          python Membangun_model/modelling.py
          echo "Menjalankan Hyperparameter Tuning..."
          python Membangun_model/modelling_tuning.py
